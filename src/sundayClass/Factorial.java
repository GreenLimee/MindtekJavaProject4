package src.sundayClass;

import java.util.Scanner;

public class Factorial {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        int num = input.nextInt();
        /*
        2. Write a code that will ask from user to enter number and then it will print factorial number.
Example:
Enter number:
5
Factorial number for 5 is 120
** Factorial number is a multiplication of numbers between 1 and number.
For example to find factorial number of 5 we should multiply numbers starting from 1 (1*2*3*4*5=120). Factorial number of 3 is 6 (1*2*3=6).
         */

       /* Scanner input = new Scanner(System.in);

        int num = input.nextInt();
        int result = 1;

        while (num>0){
            result = result * num;
            num--;
        }
        System.out.println(result);

        */
    }
}
